# 2026-08-28

## TotalLINK 仓库重命名与引用修正

- **仓库根目录重命名**：`TotalLINK_Skills` → `totallink-base`（`/Users/liuyongchao/Documents/GitHub/totallink-base`），使文件夹名与技能名匹配。不影响技能包安装/更新（走 gitee 远程，已部署实例在 `~/.workbuddy/skills/TotalLINK/` 独立 clone）。
- **相对路径引用修正**（`reimbursement-audit/SKILL.md`）：`../totallink-base/scripts/totallink_api.py` → `../scripts/totallink_api.py`（4 处代码 + 1 处前置条件）；`../totallink-base/SKILL.md` → `../SKILL.md`（2 处）。
- **原因**：`reimbursement-audit` 与 `scripts/`、`shared/` 同级，部署后依旧同级；原 `../totallink-base/` 路径在部署结构中不存在。修正后路径同时适用于源码与部署环境，且不再依赖技能 `name` 字段。
- 基础 SKILL.md 中 `参见 totallink-base Skill` 为技能名文字引用，语义正确，保留未改。

## 服务端警告（Action Warning）机制支持（commit 6af5670）

- `scripts/totallink_api.py` 新增 `DEFAULT_TIMEOUT=180`、`WARNING_ACTION="warning"` 常量，`call()` 增加 `timeout` 参数，CLI 增加 `--timeout`。
- 响应 `action=="warning"` 时识别为服务端警告（设计行为非故障），结果 JSON 增加 `_warning` 字段（`{detected:true, message}`），stderr 打印提示；只看 action 不解析 message。
- 根 `SKILL.md` 新增「服务端警告（Action Warning）」章节：识别方式、现象与应对（降速等待/超时重试/额度用尽提示）。
- README.md 目录结构注释同步为 `totallink-base/`。
- 已提交并推送到 gitee（`d9d5a8e..6af5670`）。`.codebuddy/` 为工作数据目录未提交。
