# MEMORY

## 项目约定（TotalLINK Skills 仓库）

- **仓库根目录**：`/Users/liuyongchao/Documents/GitHub/totallink-base`（2026-08-28 由 `TotalLINK_Skills` 重命名而来，目的是让文件夹名与技能名 `totallink-base` 匹配）。
- **Git 远程**：`origin` → github.com/randyliuyc/TotalLINK_Skills；`gitee` → gitee.com/randyliuyc/TotalLINK_Skill。仓库根目录改名不影响 git 仓库本身及远程配置。
- **基础技能**：根 `SKILL.md`，frontmatter `name: totallink-base`，`slug: totallink`，多平台通用（WorkBuddy AskUserQuestion 弹窗 + 其他 Agent 文字交互）。
- **目录结构**：`SKILL.md`（基础技能）、`scripts/`（totallink_api.py / parse_tools.py / preprocess.py）、`reimbursement-audit/`（报销审计）、`shared/`（email-sender、pdf-generator）。
